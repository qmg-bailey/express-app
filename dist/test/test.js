'use strict';

var assert = require('assert');

describe('Home', function () {
  describe('text', function () {
    it('should display the test Hello world!', function () {
      assert.equal(-1, [1, 2, 3].indexOf(4));
    });
  });
});